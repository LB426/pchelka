<%= form_for(@defset) do |f| %>
  <% if @defset.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@defset.errors.count, "error") %> prohibited this defset from being saved:</h2>

      <ul>
      <% @defset.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :value %><br>
    <%= f.text_area :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
